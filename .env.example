 # Where the payments provider lives (the bundled mock listens on 8001)
PROVIDER_BASE_URL=http://127.0.0.1:8001
PROVIDER_API_KEY=sk_test_demo
# Shared secret used to sign and verify webhooks
WEBHOOK_SECRET=whsec_demo
DB_PATH=payments.db
# Reject webhooks whose timestamp is older than this many seconds
WEBHOOK_TOLERANCE_SECONDS=300
